import { ClubLanding } from "@/components/club/club-landing";

export const metadata = { title: "GoldenMovies Club — Premium membership" };

export default function ClubPage() {
  return <ClubLanding />;
}
